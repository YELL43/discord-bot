meta {
  name: delete by id
  type: http
  seq: 6
}

delete {
  url: {{HOST}}/api/portals/roles/:id
  body: none
  auth: bearer
}

params:path {
  id: 5
}

auth:bearer {
  token: {{COOKIE_ID_TOKEN}}
}
